#include<iostream>
#include<cmath>
using namespace std;
int main()
{
	float a, b, c, value1, value2;
	cout<<"enter the value of a"<<endl;
	cin>>a;

	cout<<"enter the value of b"<<endl;
	cin>>b;

	cout<<"enter the value of c"<<endl;
	cin>>c;

	if((sqrt(pow(b,2)-4*a*c)<0))
	{
		cout<<"the value of x is complex"<<en
	else
		if((sqrt(

}